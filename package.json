{
  "name": "party-train",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Party Train dominoes — single-player vs AI, PlayCanvas engine",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:sim": "vitest run --reporter=verbose tests/simulation.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playcanvas": "^2.17.2"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.5",
    "vitest": "^4.1.2"
  }
}
